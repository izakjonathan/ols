export default function VersionPage() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui, sans-serif" }}>
      <h1>Øland Service v30</h1>
      <p>Landing type reduced, chevrons centered/rotating, and CTA + booking combined into one full-screen section.</p>
      <p>Production build tested successfully.</p>
    </main>
  );
}
