export default function VersionPage() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui, sans-serif" }}>
      <h1>Ølands Service v27</h1>
      <p>Vercel install fixed by downgrading npm before npm ci.</p>
      <p>Routes: /admin, /backend, /backend-home, /backend-start.</p>
    </main>
  );
}
